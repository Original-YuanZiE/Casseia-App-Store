<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="App_Store.Pages.DownloadPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:App_Store.Pages"
    xmlns:core="using:App_Store.Core"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent">

    <Grid>
        <ListView x:Name="ResList" Grid.Row="1" Margin="50" SelectionMode="None" >
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="core:DownloadTask">
                    <Button Click="Cancel_Click" Tag="{x:Bind Mode=OneWay}" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Background="Transparent" BorderBrush="Transparent" BorderThickness="0">
                        <StackPanel Orientation="Horizontal" Spacing="30" Height="100">
                            <ProgressRing IsIndeterminate="False" Value="{x:Bind Progress, Mode=OneWay}"/>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <TextBlock Text="{x:Bind Name, Mode=OneWay}" FontSize="22" VerticalAlignment="Bottom"/>
                                <TextBlock Text="{x:Bind Speed, Mode=OneWay}" FontSize="14" Grid.Row="1"/>
                            </Grid>
                        </StackPanel>
                    </Button>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
