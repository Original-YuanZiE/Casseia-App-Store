<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="App_Store.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:App_Store"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="App Store">

    <Window.SystemBackdrop>
        <MicaBackdrop Kind="BaseAlt"/>
    </Window.SystemBackdrop>

    <Grid>
        <Grid x:Name="MainGrid">

            <Grid.RowDefinitions>
                <!--标题栏-->
                <RowDefinition Height="48"/>
                <!--客户区-->
                <RowDefinition/>
            </Grid.RowDefinitions>

            <!--标题区域-->
            <Grid Grid.Row="0">
                <TextBlock x:Name="TitleText" Text="App Store" VerticalAlignment="Center" FontWeight="Bold" Margin="20,0,0,0"/>
            </Grid>

            <!--导航视图-->
            <NavigationView x:Name="MainNavView" Grid.Row="1" IsSettingsVisible="False" ItemInvoked="MainNavView_ItemInvoked">
                <NavigationView.MenuItems>
                    <NavigationViewItem x:Name="NavigateSearch" Content="搜索应用">
                        <NavigationViewItem.Icon>
                            <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE721;"/>
                        </NavigationViewItem.Icon>
                    </NavigationViewItem>
                    <NavigationViewItem x:Name="NavigateLocal" Content="本机应用">
                        <NavigationViewItem.Icon>
                            <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE719;"/>
                        </NavigationViewItem.Icon>
                    </NavigationViewItem>
                    <NavigationViewItem x:Name="NavigateDownload" Content="下载管理">
                        <NavigationViewItem.Icon>
                            <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE896;"/>
                        </NavigationViewItem.Icon>
                    </NavigationViewItem>

                </NavigationView.MenuItems>
                <Frame x:Name="MainFrame"/>
            </NavigationView>

        </Grid>


    </Grid>
</Window>
